extends ../layout/index

block contenido
    div(class="py-6")
        h1(class="text-4xl my-10 font-extrabold text-center") Bienes
            span(class="font-normal") Raices
        h2(class="text-center text-2xl font-extrabold")=pagina

        div(class="mt-8 mx-auto max-w-md")
            div(class="bg-white py-8 px-4 shadow rounded-2xl")
                form(method="post")
                    div(class="mb-4")
                        label(for="email", class="block text-sm uppercase text-gray-500 mb-2 font-bold") Email:
                        input(id="email", name="email", type="email", class="w-full px-3 py-2 border border-gray-400 rounded-md", placeholder="Ingresa tu email")
                    div(class="mb-4")
                        input(type="submit", value="Enviar Email", class="w-full bg-indigo-600 hover:bg-indigo-700 text-white py-2 uppercase font-bold rounded-md")

                    div(class="mb-4 flex")
                        div(class="flex justify-between")
                            a(href="/auth/login", class="text-gray-500 text-sm") Iniciar Sesión
                            a(href="/auth/registro", class="text-gray-500 text-sm") Registrar Cuenta