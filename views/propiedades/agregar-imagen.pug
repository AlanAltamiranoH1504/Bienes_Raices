extends ../layout/index

block styles
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/dropzone/5.9.3/dropzone.css")
    meta(name="csrf-token" content=csrf)

block contenido
    div(class="py-0")
        h1(class="text-4xl my-10 font-extrabold text-center") Bienes
            span(class="font-normal")  Raices
        h2(class="text-center text-2xl font-extrabold")=pagina

        div(class="mt-8 mb-6 mx-auto max-w-[700px]")
            div(class="bg-white py-8 px-4 shadow rounded-2xl")
                div(class="mb-4")
                    h2(class="font-bold text-center uppercase") Informacion General
                    p(class="text-gray-500 mt-2 text-sm") Añade la imagen de tu propiedad
                form(method="post", action=`/propiedades/guardar_imagen/${id_propiedad}`, class="dropzone" id="imagen")
                    input(name="_csrf", value=csrf, type="hidden", id="crsf_token")
                    input(name="id_propiedad", value=id_propiedad, type="hidden")

                div(class="mb-4 mt-4")
                    button(id="publicar", type="button", class="bg-green-600 hover:bg-green-700 text-center text-white uppercase font-bold rounded-xl w-full py-2") Publicar Propiedad


block scripts
    script(src="/js/agregarImagen.js")